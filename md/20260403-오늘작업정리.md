# 2026-04-03 오늘 작업 정리

## 📌 구현한 기능

### 1. 부서 코드(Department Code) 추가
**파일**: `User.java`, `SignupRequestDto.java`, `UserResponseDto.java`

#### 구현 내용
- User 엔티티에 `departmentCode` 필드 추가
  ```java
  @Column(name = "department_code", nullable = false, length = 2)
  private String departmentCode;
  ```

- 부서코드 Enum 생성 (`DepartmentCode.java`)
  ```
  개발팀 → 01
  유지보수팀 → 02
  모바일개발팀 → 03
  ```

- 회원가입 요청에 부서 검증 추가
  ```java
  @Pattern(regexp = "^(01|02|03)$", message = "부서 코드는 01, 02, 03 중 하나여야 합니다.")
  private String departmentCode;
  ```

- 응답 DTO에 부서명 포함
  ```java
  private String departmentCode;
  private String departmentName; // 콤보박스 표시용
  ```

#### DB 마이그레이션
- `V4__add_department_code_to_users.sql`
  ```sql
  ALTER TABLE users ADD COLUMN department_code VARCHAR(2) NOT NULL;
  UPDATE users SET department_code = '01' WHERE department_code IS NULL;
  ```

---

### 2. 특근(Overtime) 엔드포인트 권한 분리
**파일**: `OvertimeController.java`, `OvertimeService.java`

#### `/list` vs `/all` 차이

| 엔드포인트 | 권한 | 반환 데이터 | 용도 |
|----------|------|-----------|------|
| **GET /list** | USER만 접근 | 본인 특근만 | 개인 조회 |
| **GET /all** | ADMIN/TEAM_LEADER만 | 전체 특근 | 관리자 대시보드 |

#### 구현 내용

**`/list` (USER 전용)**
```java
public List<OvertimeResponseDto> getOvertimeRequests(Long accessorUserId) {
    User accessor = getUserById(accessorUserId);
    
    // USER가 아니면 거부
    if (!accessor.getUserRole().isGeneralUser()) {
        throw new BusinessException(ErrorCode.ACCESS_DENIED, 
            "관리자는 /api/v1/overtime/all 엔드포인트를 사용해주세요.");
    }
    
    // 본인 내역만 반환
    return overtimeRequestRepository.findByRequester_Id(accessor.getId())...
}
```

**`/all` (ADMIN/TEAM_LEADER 전용)**
```java
public List<OvertimeResponseDto> getAllOvertimeRequestsForAdmin(Long userId) {
    User user = getUserById(userId);
    
    // ADMIN/TEAM_LEADER만 접근
    if (!accessPolicy.canViewAllRequests(user.getUserRole())) {
        throw new BusinessException(ErrorCode.ACCESS_DENIED, "관리자 권한이 필요합니다.");
    }
    
    // 항상 전체 반환
    return overtimeRequestRepository.findAll()...
}
```

---

### 3. CORS 설정 분리
**파일**: `global/config/WebConfig.java` (새로 생성)

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("http://localhost:5173")
                .allowedMethods("GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                .allowCredentials(true);
    }
}
```

#### 이점
- CORS 설정을 `SecurityConfig`에서 분리
- 역할별 명확한 설정 구분
- 유지보수성 향상

---

### 4. 문서 업데이트

#### `02-entity.md`
- User 엔티티 명세에 `department_code` 컬럼 추가
- 부서 코드 정책(01/02/03) 명시

#### `03-rest-api.md`
- 회원가입 요청 예시에 `departmentCode` 추가
- 로그인 응답에 `departmentCode`, `departmentName` 반영
- 특근 엔드포인트 정리 (4.8.4 `/all` 섹션 추가)

#### `postman-초기테스트-가이드.md`
- 회원가입 예시에 부서코드 추가 (01/02/03)
- 특근 `/list` vs `/all` 차이 설명

---

## 🔄 변경된 파일 목록

### 신규 생성
- `src/main/java/com/minierp/backend/domain/user/entity/DepartmentCode.java`
- `src/main/java/com/minierp/backend/global/config/WebConfig.java`
- `src/main/resources/db/migration/V4__add_department_code_to_users.sql`

### 수정
- `src/main/java/com/minierp/backend/domain/user/entity/User.java`
- `src/main/java/com/minierp/backend/domain/user/dto/SignupRequestDto.java`
- `src/main/java/com/minierp/backend/domain/user/dto/UserUpdateRequestDto.java`
- `src/main/java/com/minierp/backend/domain/user/dto/UserResponseDto.java`
- `src/main/java/com/minierp/backend/domain/user/service/AuthService.java`
- `src/main/java/com/minierp/backend/domain/user/service/UserService.java`
- `src/main/java/com/minierp/backend/domain/overtime/controller/OvertimeController.java`
- `src/main/java/com/minierp/backend/domain/overtime/service/OvertimeService.java`
- `md/02-entity.md`
- `md/03-rest-api.md`
- `md/postman-초기테스트-가이드.md`

---

## 🧪 테스트 항목

### 부서 코드 저장
```
POST /api/v1/auth/signup
{
  "id": "user01",
  "name": "테스트",
  "email": "test@test.com",
  "password": "Password123!",
  "departmentCode": "02",  // ← 부서코드 선택
  "position": "사원"
}

응답:
- departmentCode: "02"
- departmentName: "유지보수팀"
```

### 특근 엔드포인트 권한 분리
```
# USER가 /list 호출 → 본인 특근만 조회
GET /api/v1/overtime/list
Authorization: Bearer {userToken}
→ 본인 특근 목록만 반환

# ADMIN이 /all 호출 → 전체 특근 조회
GET /api/v1/overtime/all
Authorization: Bearer {adminToken}
→ 전체 특근 목록 반환

# USER가 /all 호출 → 403 Forbidden
GET /api/v1/overtime/all
Authorization: Bearer {userToken}
→ 403 에러 (관리자 권한이 필요합니다)
```

---

## 📊 코드 통계

| 항목 | 수량 |
|------|------|
| 신규 파일 | 3개 |
| 수정 파일 | 10개 |
| 마이그레이션 스크립트 | 1개 |
| 문서 업데이트 | 3개 |

---

## ✅ 완료 체크리스트

- [x] 부서 코드 저장 (DB까지)
- [x] 프론트 콤보박스 값(01/02/03) → DB 저장
- [x] 응답에 부서명 표시
- [x] 특근 `/list` vs `/all` 권한 분리
- [x] CORS 설정 분리 (WebConfig)
- [x] 문서 전체 업데이트
- [x] 마이그레이션 스크립트 작성

---

## 🚀 다음 작업

1. Git에 커밋 & 푸시 (develop 브랜치)
2. 프로젝트/업무 관리 (김민희 파트)
3. 통합 테스트

---

**작성일**: 2026-04-03  
**상태**: ✅ 완료

